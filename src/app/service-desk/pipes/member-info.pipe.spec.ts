import { MemberInfoPipe } from './member-info.pipe';

describe('MemberInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
